[pytest]
markers =
    smoke: quick checks that core features work at all
    regression: tests that guard against specific bugs returning
    slow: tests that take a long time (large data, heavy processing)
